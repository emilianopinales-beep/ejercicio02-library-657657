<!DOCTYPE html>
<html lang="es">

<head>

    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Administrar imÃ¡genes</title>

    <link rel="stylesheet" href="/library/styles.css">

</head>

<body>

    <h1>Administrar imÃ¡genes</h1>

    <p>
        Administrador:
        <strong><%= usuario.nombre %></strong>
    </p>

    <p>
        <a href="/library/admin">
            Volver al panel
        </a>
    </p>

    <p>
        <a href="/library/admin/imagenes/nueva">
            Nueva imagen
        </a>
    </p>

    <hr>

    <h2>ImÃ¡genes registradas</h2>

    <table border="1" cellpadding="6">

        <thead>

            <tr>
                <th>ID</th>
                <th>Vista</th>
                <th>Libro</th>
                <th>Archivo</th>
                <th>Tipo</th>
                <th>TamaÃ±o</th>
                <th>Portada</th>
                <th>Texto alternativo</th>
                <th>Acciones</th>
            </tr>

        </thead>

        <tbody>

            <% imagenes.forEach(imagen => { %>

                <tr>

                    <td>
                        <%= imagen.imagen_id %>
                    </td>

                    <td>

                        <img
                            src="/library/<%= imagen.ruta_relativa %>"
                            alt="<%= imagen.texto_alt || imagen.nombre_archivo %>"
                            width="80"
                        >

                    </td>

                    <td>
                        <%= imagen.libro %>
                    </td>

                    <td>
                        <%= imagen.nombre_archivo %>
                    </td>

                    <td>
                        <%= imagen.mime_type %>
                    </td>

                    <td>
                        <%= imagen.tamanio_bytes %> bytes
                    </td>

                    <td>
                        <%= imagen.es_portada ? 'SÃ­' : 'No' %>
                    </td>

                    <td>
                        <%= imagen.texto_alt || '' %>
                    </td>

                    <td>

                        <a
                            href="/library/admin/imagenes/<%= imagen.imagen_id %>/editar"
                        >
                            Editar
                        </a>

                        <br><br>

                        <form
                            action="/library/admin/imagenes/<%= imagen.imagen_id %>/eliminar"
                            method="POST"
                            onsubmit="return confirm('Â¿Seguro que deseas eliminar esta imagen?');"
                        >

                            <button type="submit">
                                Eliminar
                            </button>

                        </form>

                    </td>

                </tr>

            <% }) %>

        </tbody>

    </table>

    <hr>

    <p>
        <a href="/library/admin">
            Volver al panel de administraciÃ³n
        </a>
    </p>

</body>

</html>
